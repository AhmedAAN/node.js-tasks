<html lang="en">
  <%- include("../partials/head.ejs") %>
  <body>
    <%- include("../partials/nav.ejs") %>
    <div class="container">
      <div
        class="d-flex flex-row align-items-center justify-content-between p-2"
      >
        <h1><%= user %> Notes</h1>
        <a class="btn btn-success" role="button" href="/notes/create"
          >add blogs</a
        >
      </div>
      <% for (let i = 0; i < notes.length; i++) { %> <% if (i % 3 === 0) { %>
      <div class="row">
        <% } %>

        <div class="col-md-4">
          <div class="card d-flex" style="width: auto">
            <div class="card-body">
              <h5 class="card-title"><%= notes[i].name %></h5>
              <p class="card-text"><%= notes[i].content %></p>
              <div class="text-center">
                <a href="/notes/<%= notes[i]._id %>" class="btn btn-primary"
                  >Read</a
                >
              </div>
            </div>
          </div>
        </div>

        <% if (i % 3 === 2 || i === notes.length - 1) { %>
      </div>
      <% } %> <% } %>
    </div>
    <%- include("../partials/footer.ejs") %>
  </body>
</html>
